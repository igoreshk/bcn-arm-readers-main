beacons:
  sso:
    registeredRedirectUri: http://bcn-dev.lab.epam.com/api/v1/login
    userAuthorizationUri: http://bcn-dev.lab.epam.com/api/v1/oauth/authorize

spring.data.mongodb:
  host: ecse0050040f.epam.com
  port: 32017

eureka:
  instance:
    prefer-ip-address: true

logging:
  file:
    name: logs/${spring.application.name}.log
  level:
    root: INFO

swagger:
  cors:
    allowedOrigins: http://bcn-dev.lab.epam.com, http://localhost:3000
  host: bcn-dev.lab.epam.com
